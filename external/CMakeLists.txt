# Note that these are _not_ exclude from all because we currently intend to
# install them together with the scheduler.
add_subdirectory(dataflow-scheduler-dialects)
add_subdirectory(ktir-mlir-frontend)
